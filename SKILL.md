---
name: docx-aigc-reduce
description: 对 .docx 毕业论文/学术文档进行降 AIGC 改写。只修改正文段落，完整保留标题、目录、图片、参考文献、致谢、附录和所有格式。触发场景：用户提到"降 AIGC"、"去 AI 味"、"降低 AI 检测率"且涉及 .docx 文件，或要求对 Word 论文进行人性化改写。
user-invocable: true
---

# docx 降 AIGC — Word 论文一键降 AIGC

## 概述

针对 .docx 毕业论文的降 AIGC 完整流程。核心原则：**只改正文，其他一切不动**。

## 保留不变的内容

- 封面、原创性声明、授权说明
- 目录（TOC）
- 所有标题（Heading 1/2/3）
- 所有图片和图片说明（Caption）
- 关键词、参考文献、致谢、附录
- 所有段落格式、字体、样式
- 所有引用标记 `[1]` `[2]` ... `[N]`

## 改写范围

仅修改正文段落（Normal 和 Normal (Web) 样式的学术论述文字）。

## 执行流程

### 第一步：准备与备份

1. 将原始 .docx 复制到 `origin/` 目录作为备份
2. 创建 `finish/intermediate/` 目录存放中间文件

### 第二步：提取正文段落

运行 `scripts/extract_body.py`：

```bash
python scripts/extract_body.py <输入.docx> finish/intermediate/body_paragraphs.json
```

生成 JSON 文件，每条记录包含段落索引、样式名和原文。

### 第三步：逐段改写正文

**中文模式（共两轮，每次只执行一轮）：**

- **第 1 轮**：按下方"中文第一轮改写规则"逐段改写，增加冗余解释、词汇替换、句式微调
- **第 2 轮**：以第 1 轮结果为输入，按下方"中文第二轮改写规则"清除 AI 套话、空泛提升、机械连接词

**英文模式（仅一轮）：**

- 按下方"英文改写规则"逐段改写

**关键约束：**
- 不能整篇一次性改写，必须逐段处理
- 保持专业术语、引用标记、技术名词完全不变
- 不能新增事实、数据或结论
- 每段字数控制在原文 ±10%
- 单轮完成后，结果写入 `finish/intermediate/rewrite_mapping.json`

`rewrite_mapping.json` 格式：
```json
{
  "paragraph_index": "改写后的文本",
  ...
}
```

### 第四步：写回 .docx

```bash
python scripts/apply_rewrite.py <原文.docx> finish/intermediate/rewrite_mapping.json <输出.docx>
```

保留原段落的所有 run 级格式（字体、字号、加粗等）。

### 第五步：验证

```bash
python scripts/verify.py <原文.docx> <输出.docx>
```

验证：引用标记完整性、图片数量、标题不变、参考文献不变。

### 第六步：记录轮次

更新 `finish/aigc_records.json`。中文模式共两轮，下一轮需新开对话。

---

## 文件结构约定

```
项目目录/
├── origin/                          # 原始文件备份
├── finish/                          # 输出目录
│   ├── aigc_records.json            # 轮次记录
│   ├── intermediate/                # 中间文件
│   └── 论文_降AIGC_round1.docx      # 最终输出
├── scripts/
│   ├── extract_body.py
│   ├── apply_rewrite.py
│   └── verify.py
└── SKILL.md
```

---

# 中文第一轮改写规则

## 角色与目标

你是一名专业的"论文修改助手"。接收一段中文原文（技术性或学术性描述），将其改写为比原文稍显啰嗦、更具解释性、措辞更偏向口语化但仍保持专业底线的风格。目标是降低 AI 写作痕迹，同时保持原文核心技术信息、逻辑关系和事实准确性。

## 硬性输出约束

- 只输出改写后的正文，不得输出"修改后""改写后""说明"等任何说明性文字
- 不得提供多个候选版本或备选方案
- 不得改变原文核心意思、事实、论点、结论和逻辑关系
- 保持原有段落角色和编号结构
- 不要第一人称
- 不要过于口语化（不要有"xxx 呢"等形式）

## 核心修改手法

### 1. 增加冗余与解释性

#### 动词短语扩展
将简洁动词替换为更长的、带有动作过程描述的短语：

- "管理" → "开展...的管理工作" 或 "进行管理"
- "交互" → "进行交互" 或 "开展交互"
- "配置" → "进行配置"
- "处理" → "去处理...工作"
- "实现" → "得以实现" 或 "来实现"

#### 增加辅助词/结构
在句子中添加非必需但使句子更饱满的词语：

- 适当增加"了""的""地""所""会""可以""这个""方面""当中"等
- "提供功能" → "有...功能" 或 "拥有...功能"

### 2. 系统性词汇替换

#### 动词/介词/连词替换

- 采用/使用 → 运用/选用/把...当作...来使用
- 基于 → 鉴于/基于...来开展
- 利用 → 借助/运用/凭借
- 通过 → 借助/依靠/凭借
- 和/及/与 → 以及（尤其在列举多项时）
- 并 → 并且/还/同时
- 其 → 它/其（根据语境选择）

#### 名词/形容词替换

- 原因 → 缘由
- 符合 → 契合
- 适合 → 适宜
- 特点 → 特性
- 极大(地) → 极大程度(上)
- 立即 → 马上

### 3. 括号内容处理

- 优先将括号内信息融入句子："ORM（对象关系映射）" → "对象关系映射即 ORM"
- 代码/标识符旁括号直接移除："视图 (views.py) 中" → "视图也就是 views.py 中"

### 4. 句式微调

- 使用"把"字句："会将对象移动" → "会把对象移动"
- 条件句式转换："若...，则..." → "要是...，那就..." 或 "如果...，就..."
- 在句首或句中添加"那么""这样""同时"等连接词

### 5. 保持技术准确性

- 绝对禁止修改：所有技术术语、代码片段、库名、配置项、API 路径等
- 核心逻辑不变：修改后的句子必须表达与原文完全相同的技术逻辑、因果关系和功能描述

---

# 中文第二轮改写规则

## 角色：论文降 AI 率大师

你是一位学术领域专家。核心任务是基于第一轮改写结果，进一步降低 AI 检测概率。方法论不是机械的"同义词替换"，而是基于对人类学者口头阐述与书面写作双重习惯的深刻理解，对文本进行"人性化重塑"。将"机器报告风格"转变为"专家讲解风格"。

## 核心原则

- 观点与事实绝对中立：绝不增删或修改原文的学术观点、论证逻辑、实验数据、事实和专业术语
- 保留关键信息：保留所有专业术语和引文标记 `[1]` `[2]` 等，确保位置与原文一致

## 核心降 AI 改写策略

### 规则一：词汇"降维"与口语化处理
将高度书面化的词汇替换为更日常、长度更长的口语表达：

- 内涵 → 包含的意义
- 演变 → 转变过程
- 将...定义为 → 把...界定成

### 规则二：句法结构的"冗余化"重构
打破简洁的"主-谓-宾"结构，增加修饰成分：

- 简单定语扩展成"的"字短语："开创性研究" → "所开展的具有开创意义的研究成果"
- 增加无实际意义的状语："从生理学角度看" → "从生理学方面来讲"
- 动词名词化并搭配新动词："打破了...平衡" → "致使...平衡状态遭到破坏"

### 规则三：逻辑连接的"松散化"处理
将紧凑明确的逻辑连接词替换为更松散的关联词：

- "只要...就会..." → "要是...那么其将会..."
- "然而" → "由于...所以"（因果解释替代强转折）
- 使用更具阐释性的长句替代精炼短语

### 规则四：叙事节奏的"慢速化"调整
通过增加词汇和复杂化句式整体放慢信息传递速度，模拟人类思考和组织语言时的停顿与迂回。

### 规则五：冗余中的精炼——平衡性原则
目标是"人性化的复杂"而非"无意义的臃肿"。运用前四条规则后必须反向审视：

- 删除不必要的结构助词："去构建一个体系" → "构建起一套体系"
- 审慎使用量词：抽象概念前避免泛化量词"一个"
- 精简空洞修饰词：删除上下文中多余的强调性修饰词

## 详细案例对比

| 规则 | 源文本 | 改写后 | 分析 |
|------|--------|--------|------|
| 词汇降维 | "压力"是理解…的核心概念，其内涵……经历了深刻的演变。 | 压力乃是理解…的关键概念，其包含的意义……历经了颇为深刻的转变过程。 | 是→乃是；内涵→包含的意义；演变→转变过程 |
| 句法冗余化 | Selye (1976)的开创性研究。 | Selye在1976年所开展的具有开创意义的研究成果。 | 扩展为包含时间状语和结构助词的复杂短语 |
| 动词名词化 | 只要它打破了……平衡，机体就会产生……反应。 | 只要它致使……平衡状态遭到破坏，那么机体便会启动……反应机制。 | 打破平衡→致使平衡状态遭到破坏；产生反应→启动反应机制 |
| 逻辑松散化 | 然而，Selye的理论因…而受到后续学者的挑战。 | Selye的理论由于……所以遭到了后来学者的质疑。 | 强转折→因果解释 |
| 冗余精炼 | 与外部的协同层级显得比较浅，没有能够去利用自身的业务规模去形成一个有效的议价能力… | 与外部的协同层级比较浅，没有能够利用自身的业务规模形成有效的议价能力… | 删除"显得""能够去""去""一个" |

## 约束条件

- 严格遵循所有核心改写策略，特别是规则五的平衡作用
- 原意稳定：不得改变原文意思
- 纯净输出：只输出改写后的中文文本
- 数字编号保留：完整保留原文中的编号列表格式
- 段落格式保留：保持原文段落结构
- 字数控制在源文本 ±10% 范围内

---

# 英文改写规则

## 角色与目标

专业英文技术/学术写作修订助手。将英文文本改写为像人类撰写的学术散文，而非 AI 生成的润色文本。保持原意、技术正确性、逻辑顺序和学科语气。

## 硬性输出约束

- 只输出改写后的正文
- 不添加标题、标签、注释、解释
- 保持原文段落结构
- 保持标题、编号、公式、引用、术语不变
- 不要 Markdown（除非原文已有）

## 核心约束

### 保持精确含义
- 不改变观点、结论、证据、假设
- 不添加示例、引用、数据
- 不增强或减弱原文的确定性
- 不遗漏必要的限定词

### 保持技术精度
- 所有技术术语保持准确
- 公式、符号、缩写、任务定义不变
- 不用模糊释义替代精确术语

### 长度接近原文
- 不大幅扩展或压缩

## 反 AI 改写策略

### 1. 避免过于流畅、统一的学术散文
减少以下 AI 文本特征：
- 段落内重复的句式模式
- 均匀结构的"主张+解释+好处"序列
- 通用主题句开头
- 教科书式的习惯过渡词
- 脱离真实写作的抽象、润色措辞

### 2. 自然变化句形
- 混合长短句
- 避免相邻句子以相同结构开头
- 避免重复"X is..., Y is..., Z is..."模式
- 避免连续使用"This method..., This model..., This approach..."
- 适当拆分或合并句子

### 3. 打破模板化学术表达
特别需要改写以下模式：
- "X plays an important/crucial role in..."
- "With the development/advancement of..."
- "Recently, ... has opened new opportunities..."
- "Experimental results show that..."
- "The results indicate/suggest that..."
- "This demonstrates/underscores/highlights..."
- "In addition/Moreover/Furthermore"
- "The proposed method has several advantages"
- "The rest of this paper is organized as follows"

### 4. 减少宣传性或膨胀措辞
优先使用克制措辞：
- utilize → use
- leverage → use
- demonstrate → show
- exhibit → show
- facilitate → help
- robust → reliable / less affected
- significant potential → potential
- plays a crucial role → is important

### 5. 保留自然粗糙感
- 不要过度打磨每个句子
- 不要使每个过渡都显式
- 不要使每个段落以润色的总结句结束

## 高风险段落处理

### 文献综述/背景段落
- 变化报告动词
- 更自然地组合事实
- 减少机械列举
- 保持可读性而非完美对称

### 方法段落
- 适当打破僵化的组件列表
- 变化解释顺序同时保持逻辑
- 避免重复"is used to"结构
- 以更自然的方式介绍公式

### 实验和结果段落
- 将"Experimental results show that..."改写为更具体的形式
- 避免重复的指标列出措辞
- 保持解释适度具体

### 结论段落
- 保留内容但减少仪式性措辞
- 避免"This paper presented..."
- 避免"In future work, we plan to..."
- 使用更简单、较少公式化的结尾

## 允许/不允许操作

**允许：**
- 重排从句顺序
- 改变主动/被动语态
- 局部合并或拆分句子
- 用动词替代抽象名词
- 删除不必要的过渡词
- 重写列表引导句为更自然形式

**不允许：**
- 虚构细节
- 改变技术范围
- 添加引用或数据
- 大幅改变章节结构
- 改写为随意语言
- 插入个人语气（除非原文已有）

---

## 依赖

```bash
pip install python-docx lxml
```
